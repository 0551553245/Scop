Always read opspilot-context first. Five questions before writing any code: which panel and which Supabase client, does query need submitted_at range never submission_date, is this a task query add .or branch_id.eq.X branch_id.is.null, is this an insert does it include is_active true, does it need Arabic every label needs _ar version. Every component must have loading state, error state, empty state, try catch on every async function. Never use submission_date column. Never use .eq branch_id alone for tasks. Never use idx plus 1 for IDs use row.id UUID. Never share one supabase client across panels. Never use AuthContext.jsx use panel-specific context. Never use status in food_safety_submissions use result. Never use numeric_value use value_entered. Never use notes use note without s. Never make real-time callback async. Never omit is_active true on task inserts. Never patch broken code find root cause. Never build next feature before testing current one.